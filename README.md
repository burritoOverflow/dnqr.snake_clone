Uses `cmake` and `vcpkg` building and fetching dependencies.

run `./vcpkg_configure.sh` to configure the project (fetch `GLAD` and `gflw`)

run `cmake --build ./build/` to build the target.

binary is in `./build/bin/`
